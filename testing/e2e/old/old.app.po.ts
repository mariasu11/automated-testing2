import { browser, by, element } from 'protractor';

/*export class MsdBranchesEditorPage {
  msdBranchesEditor = element(by.tagName('msd-branches-editor'));
  sideNav = this.msdBranchesEditor.element(by.tagName('msd-branches-editor-sidenav'));
  tabGroup = this.msdBranchesEditor.element(by.tagName('md-sidenav-container md-tab-header'));
  slidesOverview = this.msdBranchesEditor.element(by.tagName('msd-branches-editor-branching-editor'));

  navigateTo() {
    browser.waitForAngularEnabled(false);
    return browser.get('/');
  }

}*/
