/*import { MsdBranchesEditorPage } from './app.po';
import { browser, by, element } from 'protractor';

describe('msd-branches-editor App', () => {
  let page: MsdBranchesEditorPage;

  beforeEach(() => {
    page = new MsdBranchesEditorPage();
    page.navigateTo();
  });

  it('should display a side nav menu', () => {
    page.navigateTo();
    browser.sleep(5000);
    expect(page.sideNav.isPresent()).toBe(true);
    expect(page.sideNav.isDisplayed()).toBe(true);
  });

  it('should display a tab group', () => {
    expect(page.tabGroup.isPresent()).toBe(true);
    expect(page.tabGroup.isDisplayed()).toBe(true);
  });

  it('should display slides overview', () => {
    expect(page.slidesOverview.isPresent()).toBe(true);
    expect(page.slidesOverview.isDisplayed()).toBe(true);
  });
});
*/
